# ctx.toml — ctx-forge manifest for election-v2
# Generated by ctx-forge skill v0.1 / 2026-07-02
[ctx]
contract_version = "0.1"
conformance = "core"
generated_at = "2026-07-02T13:10:00Z"
generated_by = "ctx-forge skill v0.1 / Kun"

[project]
name = "election-v2"
primary_language = "typescript"
frameworks = ["vue>=3", "vite", "koa", "element-plus", "pinia"]
entrypoint_hint = "admin/src/main.ts"

[surface]
globs = [
  "admin/src/**/*.ts", "admin/src/**/*.vue",
  "koaLite/src/**/*.ts", "koaLite/src/**/*.js",
  "vue-cli-lazy/src/**/*.vue", "vue-cli-lazy/src/**/*.ts",
  "frontend_wx/**/*.js", "frontend_wx/**/*.vue",
  "admin/package.json", "koaLite/package.json",
  "vue-cli-lazy/package.json", "frontend_wx/package.json",
]
exclude = ["**/node_modules/**", "**/dist/**", ".ctx/**"]

[commands.map]
tier = 0
source = "tools/map.mjs"
description = "Project overview: packages, entrypoints, responsibilities"

[commands.find]
tier = 0
source = "tools/find.mjs"
description = "Semantic search over source files (symbols, routes, keywords)"

[commands.regen]
tier = 0
source = "tools/selftest.mjs"
description = "Rebuild indexes, re-run selftest"

[commands.selftest]
tier = 0
source = "tools/selftest.mjs"
description = "Run golden-question verification"

[verify]
golden = "golden.yaml"

[dropped]
schema = "需 vue-router 路由表扫描 — v1 未实现"
api = "需 Koa router 扫描 — v1 未实现"
flow = "需完整调用图 — v1 未实现"

[regen]
auto_check = true
triggers = [
  "包结构变化",
  "新模块/路由添加",
]
