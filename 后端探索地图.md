---
title: 后端探索地图
source: E:/w/0/election-v2/koaLite
explorer: 小圆圆B组·后端探测员
date: 2026-06-30
tags: [后端, koaLite, schema-v2, 端点地图, anchor-marking]
---

# 后端探索地图 · koaLite

> 只读不改·照代码现状写。锚点映射 `文件:行号`。
> 返回统一壳：`{ code, msg, data }`（见 `config/response.js`）。
> code: 0成功 / 1001参数错 / 1002不存在 / 1003未登录 / 1004权限不足 / 1005服务器错 / 1006业务错。

## 0. 地基约定（先认这三条，端点才看得懂）

- ⚡ **约定式路由**（`config/router.js:16`）：URL = `/{文件名去掉.js}/{方法名}`。
  例：`api/village-v2.js` 的 `get.list` → `GET /village-v2/list`。
- ⚡ **只有 GET / POST 两种**。POST 参数（含 `id`）全在 `ctx.request.body`；GET 参数全在 `ctx.query`。
- 🔐 **权限挂在 config 段**（`router.js:17`）：`config[方法名]` 若存在，就在处理函数前插 `requireRole(...)` 中间件。没挂 = 读放开/不校验。
  - `requireRole`（`config/requireRole.js:19`）从 `Authorization: Bearer <jwt>` 解析用户，校验 `user.role` 是否在白名单。无 token→401(1003)，角色不符→403(1004)。
  - 四角色（中文）：`超级管理` / `经办` / `审核` / `运营`；村民端角色 `villager` 无管理 token，所以村民动作（材料submit、通知read、标已读）**故意不挂权限**。

### 💾 返回结构两种形状（前端取值命根子）

| 形状 | 函数 | 实际结构 | 用在哪 |
|---|---|---|---|
| 分页 | `response.pageSuccess` (`response.js:41`) | `data: { list:[...], total, page, pageSize }` | 后台分页列表 |
| 普通 | `response.success` (`response.js:35`) | `data: <任意>`（可能是数组、对象、`{id}`、`null`） | 其余全部 |

⚠️ **铁律**：`pageSuccess` 的 data 是 **`{list,total,page,pageSize}` 对象**，不是裸数组；`success` 传数组时 data 才是裸数组。前端必须分清 `data.list` 还是 `data`。

---

## 1. 端点清单表（后端给什么的真相）

| 模块 | 方法名 | HTTP | 完整URL | 收参(query/body·字段名) | 返回 data 结构 | 权限 |
|---|---|---|---|---|---|---|
| auth | login | POST | /auth-v2/login | body: phone, password, role, villageId(审核员必填) | `{ token, user:{id,phone,role,villageId,name} }` | 读放开 |
| auth | logout | POST | /auth-v2/logout | body: 无 | `{}` | 读放开 |
| auth | verify | GET | /auth-v2/verify | header: Authorization Bearer | jwt decoded 对象 | 读放开 |
| village | list | GET | /village-v2/list | query: page, pageSize, name, type | **`{list,total,page,pageSize}`** | 读放开 |
| village | all | GET | /village-v2/all | query: 无 | `[{id,name,type}]` 裸数组 | 读放开 |
| village | add | POST | /village-v2/add | body: name, type, code, status | `{id}` | 超级管理,经办 |
| village | update | POST | /village-v2/update | body: id, name, type, code, status | `null` | 超级管理,经办 |
| village | delete | POST | /village-v2/delete | body: id | `null` | 超级管理 |
| election | list | GET | /election-v2/list | query: page, pageSize, name, villageId, electionType, status | **`{list,total,page,pageSize}`** | 读放开 |
| election | detail | GET | /election-v2/detail | query: id | 对象 + `.positions:[...]` | 读放开 |
| election | methods | GET | /election-v2/methods | query: 无 | `{村委会选举:[...],居委会选举:[...]}` 字典对象 | 读放开 |
| election | add | POST | /election-v2/add | body: villageId, name, electionType, electionMethod, content, 各时间节点, createdBy | `{id}` | 超级管理,经办 |
| election | update | POST | /election-v2/update | body: id, villageId, name, electionType, electionMethod, content, 各时间节点 | `null` | 超级管理,经办 |
| election | approve | POST | /election-v2/approve | body: id, action(approve/reject), reason(驳回必填), approverId | `null` | 超级管理,审核 |
| election | delete | POST | /election-v2/delete | body: id | `null` | 超级管理 |
| position | list | GET | /position-v2/list | query: electionId(必填) | `[{...,material_requirements:[],elected_candidates:[]}]` 裸数组 | 读放开 |
| position | detail | GET | /position-v2/detail | query: id | 对象(JSON字段已解析) | 读放开 |
| position | add | POST | /position-v2/add | body: electionId, name, quota, duty, materialRequirements, sortWeight, enrollStartAt, enrollEndAt | `{id}` | 超级管理,经办 |
| position | update | POST | /position-v2/update | body: id, name, quota, duty, materialRequirements, sortWeight, enabled, enrollStartAt, enrollEndAt | `null` | 超级管理,经办 |
| position | delete | POST | /position-v2/delete | body: id | `null` | 超级管理,经办 |
| material | list | GET | /material-v2/list | query: electionId, positionId, status | `[{...}]` 裸数组 | 读放开 |
| material | pending | GET | /material-v2/pending | query: electionId, positionId, status(默认待审核) | `[{...}]` 裸数组 | 读放开 |
| material | detail | GET | /material-v2/detail | query: id | 对象(items 已解析为数组) | 读放开 |
| material | my | GET | /material-v2/my | query: phone(必填) | `[{...,position_name,items:[]}]` 裸数组 | 读放开 |
| material | submit | POST | /material-v2/submit | body: electionId, positionId, applicantPhone, applicantName, applicantUserId, items | `{id}` | 读放开(村民端) |
| material | review | POST | /material-v2/review | body: id, action(approve/reject), rejectReason(驳回必填), reviewerId | `{candidateId}` | 超级管理,审核 |
| candidate | list | GET | /candidate-v2/list | query: page, pageSize, electionId, positionId, villageId, electionType, recommendType, source, status, name | **`{list,total,page,pageSize}`** | 读放开 |
| candidate | detail | GET | /candidate-v2/detail | query: id | 对象 + `.material{items:[]}`(自荐才有) | 读放开 |
| candidate | import | POST | /candidate-v2/import | body: electionId, positionId, phone, name, recommendType(非"村民自荐"), politics, intro | `{id}` | 超级管理,经办,运营 |
| candidate | review | POST | /candidate-v2/review | body: id, action(pass/fail), comment(fail必填) | `null` | 超级管理,经办,审核 |
| candidate | result | POST | /candidate-v2/result | body: id, elected, votes | `null` | 超级管理,运营 |
| notice | list | GET | /notice-v2/list | query: page, pageSize, electionId, type, status, title | **`{list,total,page,pageSize}`** | 读放开 |
| notice | detail | GET | /notice-v2/detail | query: id | 对象(SELECT *) | 读放开 |
| notice | types | GET | /notice-v2/types | query: 无 | `[村民组通知,议事会通知,村监会通知,村务通知]` 裸数组 | 读放开 |
| notice | add | POST | /notice-v2/add | body: title, type, content, electionId, startTime, endTime, status, isTop | `{id}` | 超级管理,运营 |
| notice | update | POST | /notice-v2/update | body: id, title, content, type, startTime, endTime, status, isTop, electionId | `null` | 超级管理,运营 |
| notice | publish | POST | /notice-v2/publish | body: id, status(草稿/待发布/已发布/已下架) | `null` | 超级管理,运营 |
| notice | delete | POST | /notice-v2/delete | body: id | `null` | 超级管理,运营 |
| notification | list | GET | /notification-v2/list | query: page, pageSize, type, status, targetRole | **`{list,total,page,pageSize}`** | 读放开 |
| notification | my | GET | /notification-v2/my | query: userId 或 phone(至少一个) | `[{...,is_read}]` 裸数组 | 读放开(村民端) |
| notification | send | POST | /notification-v2/send | body: title, content, type, sendMode, userId/targetPhones/targetRole/targetVillage(至少一个), scheduledAt(定时必填) | `{id}` | 超级管理,运营 |
| notification | read | POST | /notification-v2/read | body: messageId, userId | `null` | 读放开(村民端) |
| notification | delete | POST | /notification-v2/delete | body: id | `null` | 超级管理,运营 |
| upload | saveFile | POST | /upload/saveFile | form-data: file(multipart, 字段名 file) | `{filename, url}` | multer中间件(无角色校验) |

---

## 2. 端点形状锚点（符号标）

格式 `{module}-{method}-{seq}`。⚡URL+方法 / 💾data结构 / 🔐权限 / ⚠️特殊点。

### auth（auth-v2.js）
- `auth-login-001` ⚡ POST /auth-v2/login（`auth-v2.js:10`）💾 `{token,user{id,phone,role,villageId,name}}` 🔐放开 ⚠️ md5(password) 校验；`role==='reviewer'` 时 villageId 必填且需与库里 village_id 匹配。注意登录入参 role 用英文/前端值，库里 users.role 存中文四角色——需前后端核对（见下方关键发现）。
- `auth-logout-002` ⚡ POST /auth-v2/logout（`auth-v2.js:84`）💾 `{}` 🔐放开 ⚠️ 纯占位，无服务端 token 失效。
- `auth-verify-003` ⚡ GET /auth-v2/verify（`auth-v2.js:91`）💾 jwt decoded 🔐放开 ⚠️ 从 header 取 token 自校验。

### village（village-v2.js）
- `village-list-001` ⚡ GET /village-v2/list（`village-v2.js:10`）💾 **{list,total,page,pageSize}** 🔐放开
- `village-all-002` ⚡ GET /village-v2/all（`village-v2.js:47`）💾 裸数组 `[{id,name,type}]`，仅 status='0' 🔐放开 ⚠️ 下拉用
- `village-add-003` ⚡ POST /village-v2/add（`village-v2.js:62`）💾 `{id}` 🔐超级管理,经办
- `village-update-004` ⚡ POST /village-v2/update（`village-v2.js:79`）💾 null 🔐超级管理,经办
- `village-delete-005` ⚡ POST /village-v2/delete（`village-v2.js:97`）💾 null 🔐超级管理 ⚠️ 被 elections 引用则拒删(1006)

### election（election-v2.js）
- `election-list-001` ⚡ GET /election-v2/list（`election-v2.js:25`）💾 **{list,total,page,pageSize}**，JOIN villages 带 village_name 🔐放开
- `election-detail-002` ⚡ GET /election-v2/detail（`election-v2.js:59`）💾 对象 + `.positions:[{id,name,quota,duty,sort_weight,enabled}]` 🔐放开
- `election-methods-003` ⚡ GET /election-v2/methods（`election-v2.js:84`）💾 **字典对象** `{村委会选举:['村民直接选举'],居委会选举:[...]}` 🔐放开 ⚠️ 联动下拉
- `election-add-004` ⚡ POST /election-v2/add（`election-v2.js:91`）💾 `{id}` 🔐超级管理,经办 ⚠️ type↔method 法定校验(`isLegalMethod`)；初始 status='draft', approval_status='待审批'
- `election-update-005` ⚡ POST /election-v2/update（`election-v2.js:122`）💾 null 🔐超级管理,经办 ⚠️ 同样 type↔method 法定校验
- `election-approve-006` ⚡ POST /election-v2/approve（`election-v2.js:146`）💾 null 🔐超级管理,审核 ⚠️**特殊入参** `action`(approve/reject) + `reason`(reject必填)；approve→approval_status='已通过',status='pending'；reject→'已驳回',status='draft'
- `election-delete-007` ⚡ POST /election-v2/delete（`election-v2.js:177`）💾 null 🔐超级管理 ⚠️ 有岗位则拒删(1006)

### position（position-v2.js）
- `position-list-001` ⚡ GET /position-v2/list（`position-v2.js:17`）💾 **裸数组**，material_requirements/elected_candidates 已 JSON.parse 🔐放开 ⚠️ electionId 必填
- `position-detail-002` ⚡ GET /position-v2/detail（`position-v2.js:43`）💾 对象(JSON字段已解析) 🔐放开
- `position-add-003` ⚡ POST /position-v2/add（`position-v2.js:62`）💾 `{id}` 🔐超级管理,经办 ⚠️ 校验活动存在(elections)；materialRequirements 是 JSON 数组(报名表单)
- `position-update-004` ⚡ POST /position-v2/update（`position-v2.js:87`）💾 null 🔐超级管理,经办
- `position-delete-005` ⚡ POST /position-v2/delete（`position-v2.js:110`）💾 null 🔐超级管理,经办 ⚠️ 有材料或候选人则拒删(1006)

### material（material-v2.js）— 血缘翻转源头
- `material-list-001` ⚡ GET /material-v2/list（`material-v2.js:58`）💾 **裸数组** 🔐放开 ⚠️ 默认 status=''（全部）
- `material-pending-002` ⚡ GET /material-v2/pending（`material-v2.js:63`）💾 **裸数组** 🔐放开 ⚠️ 默认 status='待审核'
- `material-detail-003` ⚡ GET /material-v2/detail（`material-v2.js:68`）💾 对象(items 解析为数组) 🔐放开
- `material-my-004` ⚡ GET /material-v2/my（`material-v2.js:79`）💾 **裸数组**(带 position_name + items) 🔐放开 ⚠️ phone 必填(村民端)
- `material-submit-005` ⚡ POST /material-v2/submit（`material-v2.js:96`）💾 `{id}` 🔐放开(村民端无token) ⚠️**铁律**：一人一场只能报一个岗位(按 election_id+applicant_phone 查重，1006)
- `material-review-006` ⚡ POST /material-v2/review（`material-v2.js:121`）💾 `{candidateId}` 🔐超级管理,审核 ⚠️**血缘翻转核心**：`action`(approve/reject)+`rejectReason`(reject必填)。approve→事务内 promoteToCandidate(生成/复用候选人 + 回填 materials.candidate_id) + 写 logs + 发 notifications。一人一场一岗按 election+position+phone 查重复用。

### candidate（candidate-v2.js）— 血缘链终点
- `candidate-list-001` ⚡ GET /candidate-v2/list（`candidate-v2.js:11`）💾 **{list,total,page,pageSize}**，多 JOIN 带 election/position/village 名 🔐放开 ⚠️ 多维筛选(7个可选筛)
- `candidate-detail-002` ⚡ GET /candidate-v2/detail（`candidate-v2.js:45`）💾 对象 + `.material{id,items,status}`(material_id 存在才有·血缘溯源) 🔐放开
- `candidate-import-003` ⚡ POST /candidate-v2/import（`candidate-v2.js:71`）💾 `{id}` 🔐超级管理,经办,运营 ⚠️ recommendType 必须非"村民自荐"；一人一场一岗查重(1006)；source='import',status='未审核'
- `candidate-review-004` ⚡ POST /candidate-v2/review（`candidate-v2.js:97`）💾 null 🔐超级管理,经办,审核 ⚠️**特殊入参** `action`(pass/fail)+`comment`(fail必填)。pass→status='报名中'；fail→'资格作废'
- `candidate-result-005` ⚡ POST /candidate-v2/result（`candidate-v2.js:117`）💾 null 🔐超级管理,运营 ⚠️**特殊入参** `elected`(当选/落选)+`votes`(票数)，回填后 status='已公示'

### notice（notice-v2.js）— 公开公告
- `notice-list-001` ⚡ GET /notice-v2/list（`notice-v2.js:13`）💾 **{list,total,page,pageSize}** 🔐放开 ⚠️ 置顶优先排序(is_top DESC)
- `notice-detail-002` ⚡ GET /notice-v2/detail（`notice-v2.js:43`）💾 对象(SELECT *) 🔐放开
- `notice-types-003` ⚡ GET /notice-v2/types（`notice-v2.js:54`）💾 **裸数组** 4类字典 🔐放开
- `notice-add-004` ⚡ POST /notice-v2/add（`notice-v2.js:61`）💾 `{id}` 🔐超级管理,运营 ⚠️ type 必须在4类字典内(1006)；created_by 取 ctx.state.user.userId
- `notice-update-005` ⚡ POST /notice-v2/update（`notice-v2.js:81`）💾 null 🔐超级管理,运营
- `notice-publish-006` ⚡ POST /notice-v2/publish（`notice-v2.js:99`）💾 null 🔐超级管理,运营 ⚠️**特殊入参** `status`(草稿/待发布/已发布/已下架)
- `notice-delete-007` ⚡ POST /notice-v2/delete（`notice-v2.js:113`）💾 null 🔐超级管理,运营

### notification（notification-v2.js）— 私密定向通知
- `notification-list-001` ⚡ GET /notification-v2/list（`notification-v2.js:11`）💾 **{list,total,page,pageSize}** 🔐放开
- `notification-my-002` ⚡ GET /notification-v2/my（`notification-v2.js:40`）💾 **裸数组**(带 is_read 未读红点) 🔐放开 ⚠️ userId 或 phone 至少一个；查定向+广播(user_id IS NULL)+按手机号(FIND_IN_SET)
- `notification-send-003` ⚡ POST /notification-v2/send（`notification-v2.js:60`）💾 `{id}` 🔐超级管理,运营 ⚠️ 至少一个接收目标；sendMode='定时' 时 scheduledAt 必填；即时→status='已发送',定时→'待发送'
- `notification-read-004` ⚡ POST /notification-v2/read（`notification-v2.js:85`）💾 null 🔐放开(村民端) ⚠️ messageId+userId 必填；幂等写 message_reads
- `notification-delete-005` ⚡ POST /notification-v2/delete（`notification-v2.js:101`）💾 null 🔐超级管理,运营

### upload（upload.js）
- `upload-saveFile-001` ⚡ POST /upload/saveFile（`upload.js:51`）💾 `{filename,url}` 🔐multer 中间件 `upload.single('file')`（`upload.js:30`），**无角色校验** ⚠️ 文件名用原始名(同名覆盖风险)；存 public/uploads/

---

## 3. Mermaid 后端结构图

```mermaid
graph TD
    subgraph 招聘比喻血缘链
      ELEC[选举活动 elections<br/>=招聘活动·轴心]
      POS[岗位 positions<br/>=职位]
      MAT[材料 materials<br/>=简历·候选人的爹]
      CAND[候选人 candidates<br/>=录用名单·链终点]
      ELEC -->|1:N village_id挂村居| POS
      POS -->|1:N| MAT
      MAT -.->|血缘翻转 promoteToCandidate<br/>审核通过自动生成| CAND
      MAT <-.->|回填 candidate_id 闭环| CAND
      VIL[村居 villages] -->|1:N| ELEC
    end

    subgraph 路由层 _x-v2_method
      R1[/village-v2/*] --> AV[village-v2.js] --> VIL
      R2[/election-v2/*] --> AE[election-v2.js] --> ELEC
      R3[/position-v2/*] --> AP[position-v2.js] --> POS
      R4[/material-v2/*] --> AM[material-v2.js]
      R5[/candidate-v2/*] --> AC[candidate-v2.js] --> CAND
      R6[/notice-v2/*] --> AN[notice-v2.js] --> NOT[公告 notices]
      R7[/notification-v2/*] --> ANT[notification-v2.js] --> NTF[通知 notifications]
      R8[/auth-v2/*] --> AA[auth-v2.js] --> USR[用户 users]
      R9[/upload/*] --> AU[upload.js] --> FS[(public/uploads)]
    end

    AM --> MAT
    AM -.血缘翻转写.-> CAND
    AM -.审核写日志.-> LOG[日志 logs]
    AM -.审核发通知.-> NTF
    ANT -.标已读写.-> MR[已读 message_reads]
    ANT -.查未读 JOIN.-> MR
    AN -.created_by.-> USR

    classDef noroute fill:#fdd,stroke:#c00;
    class LOG,MR,USR noroute;
```

说明：路由层 → api 处理函数 → 操作的表。虚线 = 跨模块/副作用写入。红框表 = 无专属 CRUD 路由（见关键发现③）。

---

## 4. 关键发现

### ① 列表端点返回结构（前端取值至关重要·逐模块确认）

**`data:{list,total,page,pageSize}`（分页·要取 `data.list`）**——共 5 个：
- village/list、election/list、candidate/list、notice/list、notification/list（全部走 `response.pageSuccess`）

**`data:[...]`（裸数组·直接取 `data`）**——共 7 个：
- village/all、position/list、material/list、material/pending、material/my、notification/my、notice/types（全部走 `response.success` 传数组）

**`data:<对象>`（单体/字典）**：
- 各 detail（election/position/material/candidate/notice）、election/methods（字典对象）、auth/verify

⚠️ 同是"列表"，村居/选举/候选人/公告/通知用分页壳，而岗位/材料用裸数组——前端写取值逻辑必须按模块区分，不能一刀切。

### ② 带特殊入参的写操作（不是普通 CRUD）

| 端点 | 特殊入参 | 行为 |
|---|---|---|
| election/approve | `action`(approve/reject) + `reason`(reject必填) | 改 approval_status + status |
| material/review | `action`(approve/reject) + `rejectReason`(reject必填) | **血缘翻转**：通过则生候选人+回填+写日志+发通知（事务） |
| candidate/review | `action`(pass/fail) + `comment`(fail必填) | 资格审查，pass→报名中 / fail→资格作废 |
| candidate/result | `elected`(当选/落选) + `votes`(票数) | 结果回填，status→已公示 |
| notice/publish | `status`(草稿/待发布/已发布/已下架) | 上下架 |

⚠️ 注意 action 取值在不同模块不一致：election/material 用 `approve`/`reject`，candidate/review 用 `pass`/`fail`。前端别混。

### ③ 10 张表里哪些没对应路由

10 张表：users / villages / elections / positions / materials / candidates / notices / notifications / message_reads / logs。

- **logs 表**（`init_v2.sql:182`）：**只写不查**。仅 material/review 里 INSERT 一条审核日志（`material-v2.js:155`）。**没有任何查询端点**，后台看不到日志列表。
- **message_reads 表**（`init_v2.sql:172`）：无专属路由。只被 notification/read 写入（`:94`）、被 notification/my 子查询读（`:46`）。
- **users 表**（`init_v2.sql:9`）：**没有 CRUD 路由**。只有 auth-v2 的 login/verify 读它。无用户管理（增删改查角色/村民）端点——种子数据靠 SQL 直插。

### ④ 其他要点

- 返回壳判定：分页一律 `pageSuccess`，其余一律 `success`。错误走 `paramError`(1001)/`businessError`(1006·HTTP200)/`serverError`(1005)/`notFound`(1002)。前端按 `code===0` 判成功，不能只看 HTTP 状态（businessError 是 200）。
- 🔐 **读全放开**：所有 GET 端点都没挂 requireRole。任何人可读全部列表/详情。
- 🔐 **村民端动作故意放开**：material/submit、notification/read、notification/my、material/my——村民无管理 token，靠 phone/userId 入参定位。
- ⚠️ **auth/login 的 role 入参**：库里 users.role 存中文（超级管理/经办/审核/运营/villager），但 login 校验里出现 `role === 'reviewer'`（英文）判断审核员。中文角色 vs 英文 reviewer 的对应关系需前后端核对（潜在不一致点）。
- ⚠️ upload/saveFile 用原始文件名存盘（`upload.js:23`），同名会覆盖；且无角色校验。

---

## 统计

- 端点总数：**44 个**（auth 3 + village 5 + election 7 + position 5 + material 6 + candidate 5 + notice 7 + notification 5 + upload 1）
- 已挂权限(requireRole)：**21 个**
  - village 3、election 4、position 3、material 1、candidate 3、notice 4、notification 3 = 21
- 读放开/未挂校验：**23 个**（含全部 GET 21 个 + 村民端 POST：material/submit、notification/read；upload/saveFile 走 multer 但无角色校验，另计）
- 表与路由：10 张表，**7 张有专属 CRUD/读写路由**，**3 张无专属路由**（users 仅登录读、logs 仅写、message_reads 仅辅助读写）
