# 全局快照·前后端契约对照总图（2026-06-29 上帝视角）

> 谋定而后动。系统的特征是循环与内在连接——按住一个脚，别处会凸起。
> 所以先要全局快照：看清所有错位背后那**一个根**，改根，不逐个按脚。
> 数据源：前端探索地图(133锚点) + 后端探索地图(44端点)，两组小圆圆探回。

---

## 🎯 一个根（所有错位的总源头）

**前后端"data 形状契约"没统一。**

后端拦截器统一返 `{code, msg, data}`，但：
- 后端 `data` 本身有**两种形状**：分页壳 `{list,total}` / 裸数组 `[...]`
- 前端各页对 `data` 的假设**又有多种**：当数组 / 取顶层 res.xxx / 猜中英枚举

→ 裂缝在"data 到底长什么样"上，5+ 处凸起都是它。

---

## 📊 契约对照表（前端要什么 ⟷ 后端给什么）

### A. 列表类（list）——后端两种形状，前端必须分清
| 模块 | 后端返回 | 前端正确取值 | 现状 |
|---|---|---|---|
| village/election/candidate/notice/notification list | `data:{list,total}` 分页壳 | `res.data.list` | CrudPage已兼容✅ / 手写页❌假设数组 |
| position/list、material/list/pending/my、village/all、notice/types | `data:[...]` 裸数组 | `res.data` | CrudPage已兼容✅ / 手写页 res.data||[] 恰好对 |

→ **CrudPage 双轨兼容已修，覆盖两种形状**。隐患全在**手写页**（dashboard/election/materials/approvals）。

### B. 写操作返回——前端取顶层，实际在 data 里（5处P0 bug）
| 位置 | 错写 | 应为 | 后果 |
|---|---|---|---|
| election/index:386 | `res.id` | `res.data.id` | 新建选举接不上id |
| notices/index:167 | `res`判定 | `res.data` | 发布判断恒false |
| 富文本插图×2 | `res.url` | `res.data.url` | 图插不进 |
| candidates:205 | `res.message` | `res.data`/`res.msg` | 消息取不到 |

### C. 枚举真值——前后端两套，要对齐
| 维度 | 前端假设 | 后端真值 | 处理 |
|---|---|---|---|
| 选举status | in_progress / 进行中（中英混判） | 看后端实际存值 | 统一 |
| 候选人status | 列表:报名中/已公示/资格作废 详情:待审核/已通过 | 后端:未审核/报名中/已公示/资格作废 | 对齐 |
| action入参 | — | 选举/材料:approve/reject 候选人:pass/fail | 前端调用分清 |

### D. 无后端接口（前端调了、koaLite没路由）
| 前端调用 | 决策 |
|---|---|
| getElectionMethods(选举方式管理) | 改用 /election-v2/methods 字典（认知定稿:它是字段非实体） |
| logs操作日志 | 建 log-v2/list 路由（表有数据） |
| mini/* 村民端 | 待建 mini 路由 |
| archives/admins/roles/settings | 确认废弃 or 后补 |

### E. 登录残留bug
auth-v2 用英文 `reviewer`+强制villageId校验 → 删（违背"不做村居隔离"）

---

## 🛠️ 改根策略（不逐个按脚）

**根治 = 统一 data 形状契约**，分两手：
1. **前端统一取值**：所有页面取数据走"先 res.data，再判 list/数组"——CrudPage已做，手写页照搬这逻辑（甚至抽成公共函数）。一处规范，全局一致。
2. **后端可选统一**：要不要把裸数组的 position/material list 也包成 `{list,total}`？让后端形状一致，前端就不用判两种。——这个要夏夏定（改后端 vs 改前端）。

> ✅ **【已改根·2026-06-29 夏夏拍板A·治本】后端列表形状已统一**
> position/list、material/list+pending+my 从裸数组改成 `response.pageSuccess` → 返 `{list,total}`。
> 现7模块list端点形状完全一致(data:{list,total})。实测验收通过。
> 前端从此 `res.data.list` 一招通吃，不用再猜两种形状。改3处后端，胜过前端到处兼容。

**施工顺序（按系统连接、不按葫芦）**：
1. ✅ **P0 完成（2026-06-29）**：登录残留删除(审核人登录干净)+ 6处res取值错位修齐(election接id/2处插图/notices发布判定/candidates消息·统一res.data.xxx)。前端编译干净。
2. ✅ **P1 主流程巡检修复（2026-06-30）**：浏览器实测登录后主流程页不再"加载失败"。
   - 根因①：`election/index.vue` 仍调旧 `/admin/election-methods`，已改接 `/election-v2/methods` 字典；选举方式作为 elections 字段，不再当独立实体。
   - 根因②：`positions/index.vue` 在后端统一 `{list,total}` 后仍 `res.data.forEach`，已改为 `res.data.list` 并兼容 snake_case JSON 字段。
   - 根因③：`notifications/index.vue` 初始化被未迁移的 `roles/admins` 旧接口拖死，已改四角色本地常量，系统人员列表暂不阻塞通知页。
   - 根因④：`notices/index.vue` 强制按当前选举过滤，导致 `election_id=null` 的全局公告看不见；已改列表展示全局公告，新增/编辑仍可带当前活动。
   - 菜单决策：左侧隐藏废弃的"选举方式"入口，避免点进无后端旧实体页。
   - 验证：Playwright 登录 `13800000001/123456/超级管理` 巡检 `/villages /election /positions /candidates /notices /notifications`，行数分别为 2/7/3/3/1/3，均无加载失败。
3. P1剩余：手写页深层表单字段、审批/材料状态枚举、详情页字段展示继续对齐招聘模型。
4. P2：无后端模块决策（logs建路由；archives/admins/roles/settings/mini 确认废弃或后补）

## 🧪 2026-06-30 压实检查：仍存在的结构/框架 Bug

> 结论：主流程列表"看得见数据"已经成立，但系统还没到"可放心操作"。剩余问题集中在 token 存储、snake_case/camelCase 字段契约、状态机入参、旧接口入口。

### P0 框架层：登录 token 存错位置
- 位置：`admin/src/stores/auth.ts`
- 现状：拦截器返回 `{code,msg,data}`，但 authStore 写 `res.token/res.user`。
- 后果：`localStorage.token` 可能是 `undefined`；GET 因后端读放开还能看列表，但需要权限的 POST/DELETE 会带 `Bearer undefined`，写操作会失败或权限异常。
- 正确契约：`res.data.token` / `res.data.user`。

### P0 状态机入参仍错
- `updateElectionStatus(id,status)` 仍 POST `{id,status}` 到 `/election-v2/approve`，后端要求 `{id, action:'approve'|'reject', reason?}`。实测错误入参返回 400。
- `updateCandidate(id,data)` / `updateCandidateStatus` 仍可能把 `status:'已通过'/'待终审'` 等旧值打给 `/candidate-v2/review`，后端只认 `action:'pass'|'fail'`。实测错误入参返回 400。
- `reviewMaterial` 驳回场景部分页面传 `reason`，后端要 `rejectReason`。实测错误键返回 400。
- `sendNotificationNow(id)` 调 `/notification-v2/send` 只传 id，后端新建通知要求 title + 接收目标。实测返回 400。

### P0/P1 字段契约：后端 snake_case，前端多处读 camelCase
- 实测接口首行字段：
  - candidate/list: `position_id/position_name/village_name/recommend_type/review_comment`
  - material/list: `position_id/position_name/applicant_phone/applicant_name/reject_reason/created_at`
  - notice/list: `election_id/is_top/start_time/end_time/created_at`
  - notification/list: `target_phones/target_role/target_village/send_mode/scheduled_at/created_at`
- 前端仍读：`positionId/applicantName/applicantPhone/recommendType/startTime/endTime/sendMode/scheduledAt/createdAt` 等。
- 可见症状：候选人页有 3 行但"参选职位/所属村居/推荐方式"为空；材料/审批聚合可能把报名人聚到 `undefined`；通知发送时间/范围显示 `-`。
- 建议根治：在 `api.ts` 或统一 adapter 层做 snake→camel，不要每页各自猜。

### P1 旧接口入口仍在
- `/admin/candidates/*`、`/admin/admins`、`/admin/roles`、`/admin/archives`、`/admin/settings`、`/admin-v2/*`、`/mini/*`、`/log-v2/list` 多数仍 404。
- 页面层影响：候选人"材料/导入/导出/删除/重新提审"、审批页管理员下拉、归档/系统人员/角色/系统设置/日志/H5 仍不是可用闭环。

### P1 上传路径错位
- 后端实际上传端点：`POST /upload/saveFile`。
- 前端存在：Tiptap `http.post('/upload', fd)`、职位示例图 `UPLOAD_URL='/api/upload'`。
- 实测：`GET /upload` 为 404，`/upload/saveFile` 非 POST 返回 405，说明端点名必须修正到 `/api/upload/saveFile`。

### 构建工具链
- `npm exec vite build` 通过。
- `npm run build` 失败在 `vue-tsc@1.8.27 + typescript@5.9.3 + Node v24.16.0`：`Search string not found: "/supportedTSExtensions = .*(?=;)/"`。
- 这是框架工具链兼容问题，当前无法用完整 TypeScript 检查兜底。

## ✅ 2026-06-30 P0/P1 修复回收：数据结构/框架主线压实

> 结论：本轮已把“能看列表但写操作/手写页可能炸”的 P0/P1 主线收住。左侧主流程页面与选举详情页经浏览器实测无加载失败、无 4xx API 错误。

### 已修复
- `auth-token-store-20260630`：`admin/src/stores/auth.ts` 改为从 `{code,msg,data}` 的 `data.token/data.user` 存登录态。实测 `localStorage.token` 不再是 `undefined`。
- `api-adapter-20260630`：新增 `admin/src/api/adapter.ts`，统一做 snake_case → camelCase 兼容别名。候选人、材料、公告、通知、职位、选举 list/detail 返回给页面时同时保留原字段和 camel 字段。
- `api-status-machine-20260630`：`api.ts` 将旧页面调用翻译为后端真实契约：
  - election approve：`status/已驳回` → `action approve/reject`
  - candidate review：中文旧状态 → `action pass/fail + comment`
  - material reject：`reason` → `rejectReason`
  - notification send：`phones` → `targetPhones`
- `upload-path-20260630`：富文本与职位示例图上传统一改为 `/api/upload/saveFile`。
- `old-api-buttons-20260630`：候选人页、选举详情页、通知页收掉会触发旧 `/admin*`/`admin-v2*`/伪重发的按钮；侧栏隐藏未迁移的归档、系统人员、角色、设置、日志入口，只保留当前已压实主线。
- `approval-admins-20260630`：审批页不再首屏请求 `/admin/admins`，用当前登录人兜底经办人列表，避免“加载失败”。

### 验证
- `node admin/scripts/contract-smoke.mjs`：通过，覆盖登录 payload、字段归一化、状态机 payload 翻译。
- `npm exec vite build`：通过。
- Playwright 登录 `13800000001 / 123456 / 超级管理` 巡检：
  - `/dashboard` 1 行
  - `/villages` 2 行
  - `/election` 7 行
  - `/positions` 3 行
  - `/materials` 2 行
  - `/candidates` 3 行
  - `/approvals` 0 行但无加载失败
  - `/notices` 1 行
  - `/notifications` 3 行
  - `/election/1` 11 行
- 全部巡检页：无“加载失败”、无可见 404、无 4xx API 响应。
- 构建产物 `admin/dist` 已清理，避免把临时产物当源码交接。

### 仍属 P2/待决策
- `api.ts` 里仍保留若干未迁移函数定义（archives/admins/roles/settings/logs/mini、candidate import/export/delete 等），但主线页面入口已收掉；后续要么建后端，要么正式废弃。
- `npm run build` 仍受 `vue-tsc + TypeScript + Node 24` 兼容问题影响；当前以 `npm exec vite build` 作为前端构建验证。

---

## 🔍 锚点索引（grep可定位）
- 前端锚点：前端探索地图.md（133个·{page}-{module}-{function}-seq）
- 后端锚点：后端探索地图.md（44个·{module}-{method}-seq）
- 任一处出错，grep 锚点ID 定位前后端两端。
