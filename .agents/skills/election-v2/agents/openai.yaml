interface:
  display_name: "Election V2"
  short_description: "Repo-specific patterns and workflows for election-v2"
  default_prompt: "Use the election-v2 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true