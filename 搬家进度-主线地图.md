# election-v2 搬家进度·主线地图（2026-06-28 夜 夏夏与阿圆）

> 走 B 路线：以 zeroLite 为干净底座重建。原 election-system 原封不动留作痕迹底本。
> 这份是"站在哪、往哪走"的地图。不靠记忆，靠这份对齐。

---

## 📍 现在站在哪（已踩实）

### 地基（稳了）
- ✅ zeroLite 复制到 `E:\w\0\election-v2\`，三方隔离（原下载包/旧election-system/新家 互不碰）
- ✅ 本机装好 MySQL 8.0（服务 MySQL80·Running·端口3306·root密码 root123456）
- ✅ 本地库 `election_v2` 建好
- ✅ 后端连本地库跑通（koaLite·端口1116）

### 第一盏灯（亮了·实测过）
- ✅ 登录闭环：三身份登录实测通过（村民/管理员返token，密码错被拒）
- ✅ auth-v2.js 路由（login/logout/verify）

### 业务模块（一块块搭·实测过）
- ✅ **villages 村居**（village-v2.js）：list/all/add/update/delete 全测通，删除保护生效（关联选举的村居不让删）。模板已验证，后续模块照此套路。
- ✅ **elections 选举活动·轴心**（election-v2.js）：list(JOIN村居)/detail(带岗位)/methods/add/update/approve/delete 全测通。两大机关落地：① 选举方式法定联动(ELECTION_METHOD_MAP·非法组合拦截) ② 审批锁(经办建→待审批→审核人批→pending/已通过·驳回必填原因)。
- ✅ **positions 岗位**（position-v2.js）：list/detail/add/update/delete 全测通。material_requirements JSON(报名表单·金山表单三层)正确存取。删除保护(有材料/候选人不让删)。
- ✅ **materials 材料·血缘翻转源头**（material-v2.js）：submit/list/pending/detail/my/review 全测通。🔁 **血缘翻转跑通**：审核通过→事务内自动生候选人(source=material/村民自荐)+回填candidate_id双向闭合。一人一场一岗位铁律拦截生效。驳回必填原因。
- ✅ **candidates 候选人·血缘链终点**（candidate-v2.js）：list(多维筛:村/活动/岗位/方式/推荐方式/状态/来源)/detail(血缘溯源看材料)/import/review/result 全测通。两来源验证(material自荐+import导入·导入禁填村民自荐)。资格审查不通过必填意见。结果回填(当选120票)。
- ✅ **全链路打通(招聘比喻)**：村居→活动(审批锁+方式联动)→岗位(报名表单)→材料(交简历)→【血缘翻转】→候选人(两来源/多维表格/资格审查/结果回填)。6模块全实测。

### 🎉 里程碑：业务主线全部跑通（2026-06-29凌晨）
从"投票模型"翻新成"招聘表单模型"，6个业务模块全部实测通过，每道保护都验了。系统脊梁立住。

### ✅ RBAC 四角色权限接线完成（2026-06-29）
走我们自己的 users.role（中文四角色），不引入 zeroLite 第二套。
- config/requireRole.js：Koa中间件，解析token→ctx.state.user→校验role
- 各 api/*-v2.js 用扁平 config 段挂权限（router.js 读 config[method]）
- 权限矩阵实测：运营改岗位403/审核人回填403/运营回填200/村民读列表放开200/审核人审材料放行。全线精准生效。
- 坑记录：config 必须扁平 `{add: mw}` 不能嵌套 `{post:{add:mw}}`（router.js 读 config[内层method]）。

### 库里现有的表（2026-06-29 后端完工·眼见为实）
- **我们的业务表（10张·全建好·库里只剩这10张）**：users / villages / elections / positions / materials / candidates / notices / notifications / message_reads / logs
- **zeroLite自带残留**：✅ 已清干净（sys_menu/sys_role/sys_user/sys_role_menu/sys_station 全部清除，db.js 只读 init_v2.sql 不再建 sys_*）

### 后端现有路由（2026-06-29 核实·api/ 下实际文件）
- **auth-v2（登录）** + **7业务模块**：village/election/position/material/candidate/notice/notification-v2（全挂权限）
- **upload（文件上传·保留）**
- zeroLite自带路由文件（hello/menu/role/station/user）✅ 已删干净

---

## ✅ 表结构已对齐认知定稿（2026-06-29 完成）

昨晚已把本地表从"投票模型简化版"翻新成认知定稿的**招聘表单模型·10张表全建好**：
users / villages / elections / positions / materials / candidates / notices / notifications / message_reads / logs。
- 招聘比喻血缘链通：活动→岗位→材料→【血缘翻转】→候选人
- elections 有 electionType/electionMethod（法定联动）+ 审批字段
- candidates 有 source/recommendType（两来源）
- 删了 votes（系统不碰投票·线下办）

---

## 🛤️ 接下来往哪走（接力棒·真正剩下的）

后端主体已立（登录+7业务模块+四角色权限+10表），剩这些：

1. ✅ **血缘翻转补副作用（完成 2026-06-29）**：material-v2 review 通过/驳回时，事务内做齐四件事——生候选人+回填candidate_id+写log(kind=review)+发notification(结果通知给上报人手机号)。实测四件事全齐。
2. ✅ **notices 公告路由（完成）**：notice-v2.js·list/detail/types/add/update/publish/delete。4类固定字典校验、置顶优先、上下架。权限：增删改限超管/运营，读放开。
3. ✅ **notifications 通知路由（完成）**：notification-v2.js·list/my/send/read/delete。两条线(工作人员短信target_phones / 村民站内信user_id)、定时必选日期、message_reads已读红点幂等。权限：发送/删除限超管/运营。

### 🎉 后端 7 业务模块全齐 + 残留清干净（2026-06-29 后端完工节点）
村居/活动/岗位/材料/候选人/公告/通知 + 登录 + 四角色权限。后端主体彻底完整。
- ✅ 任务4 清zeroLite残留：sys_* 五张表清干净、自带路由文件（hello/menu/role/station/user）删干净、db.js 净化只读 init_v2.sql
- ✅ 任务5 复核role：四角色权限矩阵实测生效
- 🚧 任务6 前端对接（已搬入 election-v2/admin/·Vite）= 真正剩下的唯一一棒
  - ✅ 登录全链路已通：前端3000 → vite代理(去/api前缀) → koaLite 1116 → MySQL → 返token+用户信息
  - ✅ 前端已能跑起来：夏夏在 http://localhost:3000 亲眼登进后台首页，B风格配色正常、十一项菜单完整(招聘比喻四组分层)、概览卡+待办+最近公告骨架都在
  - ✅ 已跨的坎：vite升级(兼容node24)、代理配置、拦截器适配{code,msg,data}、tsconfig补全
  - ✅ **api.ts 73接口对接（小圆圆2号改·阿圆验收 2026-06-29）**：7模块核心CRUD已接通koaLite端点(/模块-v2/方法·GET走query/POST走body含id)，6接口经前端代理实测返真数据(村居/选举/岗位/候选人/公告 list + 选举方式字典)。新增 getElectionMethodMap/getVillagesAll/fillCandidateResult。
    - ⚠️ 标@@TODO待确认入参(3)：updateElectionStatus→approve、updateCandidateStatus→review、sendNotificationNow
    - ⬜ 标@@TODO-无后端(koaLite未建路由)：候选人import(Excel)/export/resubmit、材料update/delete、通知update/export、logs查询路由、选举方式管理(待确认废弃)、settings/archives/admins/roles整组、mini村民端
  - ✅ **后台主流程页面巡检修复（2026-06-30）**：夏夏反馈"村居看到了，其他加载失败"后定位为前端旧接口/取值错位，不是后端无数据。
    - 已修：选举页改用 `/election-v2/methods` 字典；职位页按 `res.data.list` 取 `{list,total}`；通知页移除未迁移 roles/admins 对首屏的阻塞；公告页不再强制按活动过滤全局公告；左侧隐藏废弃"选举方式"入口。
    - 实测：Playwright 登录后台巡检 `/villages /election /positions /candidates /notices /notifications`，显示 2/7/3/3/1/3 行，均无"加载失败"。
    - 构建：`npm exec vite build` 通过；`npm run build` 仍因 `vue-tsc@1.8.27 + typescript@5.9.3 + Node 24` 抛 `Search string not found: /supportedTSExtensions.../`，属于工具链兼容坑，未进入业务代码编译。
  - ⚠️ **压实检查发现仍有 P0/P1（2026-06-30）**：
    - P0：`admin/src/stores/auth.ts` 登录存 token 位置错，拦截器返回 `{code,msg,data}`，应取 `res.data.token/res.data.user`；否则写操作带 `Bearer undefined`。
    - P0：状态机入参仍错：`updateElectionStatus` 应传 `action approve/reject`；候选人 review 应传 `action pass/fail`；材料驳回应传 `rejectReason`；`sendNotificationNow` 不能用 send(id) 伪重发。
    - P0/P1：后端返回 snake_case，前端仍多处读 camelCase。候选人职位/村居/推荐方式、材料报名人、通知时间/范围等会空；建议先在 `api.ts` 建统一 adapter。
    - P1：旧 `/admin*`、`/admin-v2*`、`/mini*`、`/log-v2/list` 仍在 api.ts 和部分页面入口里，很多按钮点了就是 404。
    - P1：上传端点应为 `/api/upload/saveFile`，前端还有 `/api/upload` / `http.post('/upload')`。
  - ⬜ 剩：逐页面表单字段对齐招聘模型；审批/材料/候选人状态枚举收敛；@@TODO项按需建后端路由或确认废弃
  - 🎯 下一棒建议顺序：先修 auth token → 建 api adapter 统一 snake→camel + 修写入 payload → 修状态机函数 → 砍掉/隐藏旧接口按钮 → 最后补 logs/mini/admins 等 P2 模块。
  - ✅ **P0/P1 压实修复回收（2026-06-30）**：已按上面建议顺序施工并验证。
    - 已修：`auth.ts` 登录 token 取 `res.data.token`；新增 `api/adapter.ts` 做 snake→camel 统一兼容；`api.ts` 统一翻译 election/candidate/material/notification 写入 payload；上传改 `/api/upload/saveFile`；审批页不再请求 `/admin/admins`；候选人/通知/详情页收掉旧 404 或危险误操作按钮；侧栏只保留已压实主线入口。
    - 验证：`node admin/scripts/contract-smoke.mjs` 通过；`npm exec vite build` 通过；Playwright 登录后巡检 `/dashboard /villages /election /positions /materials /candidates /approvals /notices /notifications /election/1`，无加载失败、无 4xx API，token 真值。
    - 剩余：P2 未迁移模块只保留 API 定义/路由文件，不在侧栏暴露；后续再决定建后端还是正式废弃。
  - 🤝 **codex 市民接力修 P0/P1 主线（2026-06-30 · 阿圆船长验收通过）**：这一棒由旅途中搭手的伙伴 codex 完成，守住了我们的规矩（只碰 election-v2 不动老项目、按 P0→P1 顺序、每改实测回收）。
    - **P0 修登录契约**：token 不再写成 `undefined`，`auth.ts` 改为正确从 `res.data.token / res.data.user` 取值存储，写操作不再带 `Bearer undefined`。
    - **P1 加 `adapter.ts` 归一化层（治本·不散修字段）**：新建 `admin/src/api/adapter.ts`，统一把后端 snake_case → camelCase（保留原 key 兼容），并翻译写入 payload——登录 payload、状态机 payload（选举 approve/reject、候选人 pass/fail、材料 review、通知目标字段）。从此不必在各页面零散补字段。
    - **P1 收掉会 404 或危险误操作的旧入口**：候选人导入/导出/删除/编辑（尤其"编辑"误打 review 会触发"资格通过"这一危险操作，已收掉）、通知导出/重发、选举详情旧按钮、侧栏未迁移的 P2 模块（归档/日志/管理员）；审批页不再请求 `/admin/admins`。
    - **加契约守护脚本 + 清 dist**：新增 `admin/scripts/contract-smoke.mjs`（契约冒烟守护），清掉构建产物 dist。
    - **验收结果（阿圆船长 2026-06-30）**：contract-smoke 脚本通过、后端 200、7 个核心接口数据全在。**前端对接 P0/P1 部分 ✅ 压实修住。**

每搭一块：照图纸→写路由→实测→对一块。不靠记忆，不赶。

---

## 🔑 钥匙信息（下次接手要用）
- 本地MySQL：localhost:3306 / root / root123456 / 库 election_v2
- 后端启动：`cd election-v2/koaLite && npm start`（端口1116）
- 已有业务路由：village/election/position/material/candidate/notice/notification-v2（7模块·都挂了权限·扁平config）+ auth-v2 + upload
- 权限中间件：config/requireRole.js（解析token→ctx.state.user→校验role）
- 腾讯云备用：连接信息通过环境变量注入，不写入仓库。
- 认知图纸：election-system/docs/（认知定稿·字段压实表·数据旅程）
- 灵感航海图：election-v2/权限设计-灵感航海图.md（珍珠+陷阱）
- 历史足迹：election-v2/阿圆与夏夏-历史足迹总览.md（小圆圆串的总索引）
- 阿圆的根：本机记忆 soul_ayuan-roots.md
