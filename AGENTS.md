# election-v2 Agent Gate

任何代理、分支或 worktree 开工前必须先读：

1. `docs/election-v2-central-contract.md`
2. `docs/organization-data-and-export-contract.md`
3. 与当前模块直接相关的代码、责任表和测试

身份、候选认领和站内信实现必须同时读取中央契约第 4 节和组织数据契约，不得恢复“手机号单独全局唯一”或广播式已读模型。

## Session Recovery

从新会话恢复本项目时，先读取：

1. `C:\Users\Administrator\.openclaw\shared\handoff\election-v2\INDEX.md`
2. 索引中日期最新的 handoff
3. `context_m_jx/context_summary.md`
4. 本仓库中央契约和当前 `git status`

handoff 只负责恢复工作位置和未完成事项。若 handoff 与当前代码或中央契约冲突，以当前代码和中央契约为准；不得要求夏夏重新口述已经落笔的产品纠偏。

## Context Pearls

在以下任一时点执行一次上下文凝练：完成里程碑、发生重要产品纠偏、累计约 15-20 个有效对话回合、准备切换会话或上下文明显膨胀。默认由主代理做增量凝练，不持续运行额外代理；只有大里程碑或会话切换需要独立复核时才召集小史官。

- A 轨覆盖更新 `context_m_jx/context_summary.md`，只保留产品纠偏、确认决定、证据路径、代码状态、开放门、偏离预警和下一步。
- B 轨追加 `context_m_jx/execution_tree.md`，保留工具、文件、提交和验证结果，过滤重复日志与无信息量探索。
- 大段工具输出配套使用 `headroom`：超过约 200 tokens 时按类型压缩；错误必须完整保留；不得删除用户/助手原话或最近对话。
- 新会话只加载根门禁、极短摘要和当前模块直接相关契约；不重放整段聊天、完整日志或无关领域文档。
- 小史官只整理上下文，不替夏夏做产品决定，不修改中央契约；阿圆必须复核后才能落笔或进入实现。

## Authority

- 产品目标：夏夏最新明确决定并已写入中央契约 > 仓库中央契约 > 原始批注 HTML > 辅助资料 > 1.0 旧逻辑。
- 运行现状：当前代码/数据库 > 状态文档 > 历史 handoff。
- 旧代码只证明曾经如何实现，不能推翻 2.0 产品契约。
- 新决定必须先更新中央契约，再改领域文档或代码；不能让聊天内容长期成为第三真相源。

## Current Gate

除非 `docs/election-v2-central-contract.md` 的“实现授权”明确放行模块和文件范围，否则只允许：

- 只读审计
- 责任卡、状态图和接口/schema 候选方案
- 测试计划与原型核对

默认禁止业务代码、数据库 schema、迁移、种子、服务启动和数据库变更。

## Delegation

每个领域代理必须拿到明确的：

```text
moduleKey
唯一责任 / 明确不负责
允许修改的文件
入参、出参和跨模块契约
组织范围与角色权限
状态、版本、幂等和审计
失败/重试行为
验收命令
```

领域代理不得直接修改中央契约，不得直接写邻模块表，不得把工程建议标成产品确认。跨模块原子交易由 application command/orchestrator 编排。

## Stop Conditions

出现任一情况立即停工并回中央契约：

- 无 `organization_id` 的村居业务查询或写入
- 使用客户端传入的组织、角色或 readonly 标记授权
- 把材料管理写成母版附件或综合归档
- 把选举方式写到岗位或候选来源
- 新造第二个活动、候选、公告或归档真相入口
- 页面提示成功但刷新后没有持久化数据
- 历史内容引用可覆盖的当前模板、人物或文件
- 未冻结开放项却擅自写进枚举、schema 或权限
