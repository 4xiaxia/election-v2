---
title: 前端探索地图
author: 小圆圆A组·前端探测员
date: 2026-06-30
scope: E:/w/0/election-v2/admin/src
tags: [前端, API锚点, 探索地图, schema-v2]
---

# 前端探索地图

> 只读探测，照代码现状记录。锚点 ID 格式：`{page}-{module}-{function}-{seq}`，可回找到 `文件:行号`。
> 关联文档：[[后端探索地图]]（若存在）、[[reference_election-truth-files]]

## 0. 全局机制（先懂这两条，再看每页）

### 0.1 HTTP 响应格式（`api/index.ts`）
- baseURL = `/api`，请求拦截器注入 `Authorization: Bearer <token>`。
- 响应拦截器适配 koaLite 格式 `{code, msg, data}`：
  - `code === 0` → 返回**整个 body**（`{code,msg,data}`），页面取 `.data`。
  - `code !== 0` → `ElMessage.error` + reject。
  - 无 `code` 字段 → 原样返回 body（兜底）。
- ⚠️ 关键推论：页面拿到的 `res` 是 `{code,msg,data}`，真实业务数据在 `res.data`。任何写 `res.id`、`res.url`、`res.message` 的地方都拿不到值（应是 `res.data.xxx`）。

### 0.2 CrudPage 取数兼容逻辑（`components/CrudPage.vue:124-141`）
```
const d = res?.data ?? res;
if (Array.isArray(d)) { tableData = d; total = d.length; }     // ① 直接数组
else { tableData = d?.list || []; total = d?.total || 0; }      // ② {list,total} 分页
```
- CrudPage **同时兼容**「`res.data` 是数组」和「`res.data` 是 `{list,total}`」两种格式，比较稳。
- 但**手写页面**（非 CrudPage）几乎都假设 `res.data` 直接是数组（`res.data || []`），不兼容 `{list,total}`。这是前端最大的一处格式假设分裂。

---

## 1. 页面清单表

| 页面 | 路径 | 用CrudPage? | 调用的 api 函数 | 角色权限 |
|---|---|---|---|---|
| 登录 | `/login` | 否 | `login` | 公开 |
| 首页概览 | `/dashboard` | 否 | `getElections` `getMaterials` `getNotices` | 超级管理/经办/审核/运营 |
| 村居管理 | `/villages` | ✅ | `getVillages` `createVillage` `updateVillage` `deleteVillage` | 超级管理/经办 |
| 选举管理 | `/election` | 否 | `getElections` `createElection` `updateElection` `updateElectionStatus` `deleteElection` `getVillages` `getElectionMethods⚠️` `getPositions` `getCandidates` `getNotices` `getMaterials` | 超级管理/经办/审核/运营 |
| 选举详情 | `/election/:id` | 否 | `getElections` `updateElection` `getElectionMethods⚠️` `getPositions` `createPosition` `updatePosition` `deletePosition` `getCandidates` `createCandidate` `updateCandidate` `deleteCandidate⚠️` `importCandidates⚠️` `exportCandidates⚠️` `getMaterials` `reviewMaterial` `getNotices` `createNotice` `updateNotice` `deleteNotice` `publishNotice` `getNotifications` `createNotification` | 超级管理/经办/审核/运营 |
| 职位管理 | `/positions` | ✅（含活动选择器外壳） | `getElections` `getPositions` `createPosition` `updatePosition` `deletePosition` | 超级管理/经办 |
| 候选人管理 | `/candidates` | ✅（含活动选择器外壳） | `getElections` `getCandidates` `createCandidate` `updateCandidate` `deleteCandidate⚠️` `resubmitCandidate⚠️` `importCandidates⚠️` `exportCandidates⚠️` `getPositions` `getVillages` `checkCandidateMaterials⚠️` | 超级管理/经办/审核/运营 |
| 审批管理 | `/approvals` | 否 | `getElections` `getAdmins⚠️` `getPositions` `reviewMaterial` `updateCandidate` | 超级管理/经办/审核 |
| 材料审核 | `/materials` | 否 | `getElections` `getPositions` `getMaterials` `reviewMaterial` | 超级管理/经办/审核 |
| 公告管理 | `/notices` | ✅（含活动选择器外壳） | `getNotices` `createNotice` `updateNotice` `deleteNotice` `publishNotice` `getElections` + `http.post('/upload')` | 超级管理/经办/运营 |
| 通知设置 | `/notifications` | 否 | `getNotifications` `createNotification` `sendNotificationNow` `exportNotifications⚠️` `getElections` `getVillages` `getRoles⚠️` `getAdmins⚠️` | 超级管理/经办/运营 |
| 选举方式 | `/election-methods` | 否 | `getElectionMethods⚠️` `createElectionMethod⚠️` `updateElectionMethod⚠️` `reviewElectionMethod⚠️` `deleteElectionMethod⚠️` | 超级管理/经办 |
| 历史归档 | `/archives` | ✅ | `getArchives⚠️` `createArchive⚠️` `updateArchive⚠️` `deleteArchive⚠️` `exportArchives⚠️` `exportFullArchives⚠️` | 超级管理/经办/运营 |
| 系统人员 | `/admins` | ✅ | `getAdmins⚠️` `createAdmin⚠️` `updateAdmin⚠️` `deleteAdmin⚠️` `getRoles⚠️` | 超级管理 |
| 角色权限 | `/roles` | ✅ | `getRoles⚠️` `createRole⚠️` `updateRole⚠️` `deleteRole⚠️` | 超级管理 |
| 系统设置 | `/settings` | 否 | `getSettings⚠️` `updateSettings⚠️` | 超级管理 |
| 操作日志 | `/logs` | ✅（只读） | `getOperationLogs⚠️` | 超级管理/经办 |
| H5·首页 | `/m` | 否 | `miniGetNotices⚠️` `miniGetElections⚠️` | 公开（免登录） |
| H5·公告 | `/m/notices` | 否 | `miniGetNotices⚠️` `miniGetElections⚠️` | 公开（免登录） |
| H5·选举方式 | `/m/elections` | 否 | `miniGetElectionMethods⚠️` | 公开（免登录） |
| H5·候选人 | `/m/candidates` | 否 | `miniGetCandidates⚠️` `miniGetElections⚠️` | 公开（免登录） |

> ⚠️ 标记 = 该 api 函数在 `api/api.ts` 里标了 `@@TODO-无后端`（详见第4节）。

---

## 2. 前端「要什么」的 API 锚点清单（重点）

> 符号：⚡API接入点 / 💾数据绑定 / 🔐权限 / ⚠️风险点

### 登录 `views/login/index.vue`
- ⚡ `login-auth-login-001` `login(phone,password,role,villageId?)` → `POST /auth-v2/login`（`api.ts:256`）
- 💾 `login-auth-form-002` 三身份入参：手机号 + 密码 + 身份（超级管理/经办/审核/运营）。`审核` 身份额外要 `villageId`（`login/index.vue:28-30`）。
- 🔐 `login-auth-roles-003` 身份枚举写死在下拉框：超级管理/经办/审核/运营（`login/index.vue:16-19`），与 router 角色矩阵一致。

### 首页概览 `views/dashboard/index.vue`
- ⚡ `dashboard-stats-getElections-001` `getElections()` → `GET /election-v2/list`（`dashboard:98`）
- ⚡ `dashboard-stats-getMaterials-002` `getMaterials()` → `GET /material-v2/list`（`dashboard:98`）
- ⚡ `dashboard-stats-getNotices-003` `getNotices({page:1,size:5})` → `GET /notice-v2/list`（`dashboard:98`）
- 💾 `dashboard-bind-arrays-004` 三个都按 `res.data || []` 当**数组**用（`dashboard:100-102`）。
- ⚠️ `dashboard-risk-status-005` 状态值双轨假设：同时判 `'in_progress'`/`'进行中'`、`'已发布'`/`'published'`（`dashboard:106-108`）。前端不确定后端回中文还是英文枚举。

### 村居管理 `views/villages/index.vue`（CrudPage）
- ⚡ `village-list-getVillages-001` `getVillages` → `GET /village-v2/list`（`api.ts:35`）
- ⚡ `village-crud-002` create→`/village-v2/add`、update→`/village-v2/update`、delete→`/village-v2/delete`（`api.ts:37-39`）
- 💾 `village-bind-003` 经 CrudPage 兼容数组/`{list,total}`。字段 `name` `type`（`villages:12-15`）。

### 选举管理 `views/election/index.vue`
- ⚡ `election-list-getElections-001` `getElections()` → `GET /election-v2/list`（`election/index:302`）
- ⚡ `election-form-getVillages-002` `getVillages()` 填村居下拉（`election/index:302`）
- ⚡ `election-form-getElectionMethods-003` `getElectionMethods()` → `GET /admin/election-methods`（`election/index:302`）
- ⚡ `election-crud-create-004` `createElection` → `POST /election-v2/add`（`election/index:385`）
- ⚡ `election-crud-update-005` `updateElection` → `POST /election-v2/update`（`election/index:381`）
- ⚡ `election-status-006` `updateElectionStatus(id,status)` → `POST /election-v2/approve`（`election/index:383,390`）
- ⚡ `election-crud-delete-007` `deleteElection` → `POST /election-v2/delete`（`election/index:403`）
- 💾 `election-bind-list-008` `eleRes.data || []` 当**数组**（`election/index:304`）。行字段：`name` `village` `electionMethodName` `date` `status`（`election/index:21-31`）。
- 💾 `election-bind-form-009` 表单提交字段：`name` `villageId` `electionMethodId` `timeRange` `date` `electionEndDate` `location` `description` + tiptap 富文本 `content`（`election/index:45-91`）。
- ⚠️ `election-risk-createId-010` **取值错位**：`createElection` 后 `if (res?.id) editForm.id = res.id`（`election/index:386-387`）。拦截器返回 `{code,msg,data}`，真实 id 应在 `res.data.id`。`res.id` 恒为 undefined → 新建后接不上状态流转/详情跳转。
- ⚠️ `election-risk-methodDep-011` 选举方式下拉依赖 `getElectionMethods`（`/admin/election-methods` 标 @@TODO-无后端）。**新建选举的核心表单字段依赖一个无后端接口**。
- ⚠️ `election-risk-uploadUrl-012` 富文本插图 `http.post('/upload')` 后用 `res.url`（`election/index:263`），同样应为 `res.data.url`。
- 🔐 `election-roles-013` 四角色都可进入；删除/编辑按钮无前端角色细分（`router:19`）。

### 选举详情 `views/election/detail.vue`
- ⚡ `electionDetail-load-getElections-001` `getElections({id})` → `GET /election-v2/list`，再前端 `.find(e=>e.id===id)`（`detail:463-464`）。⚠️ 用 list+前端过滤而非 `getElection(id)` detail 接口。
- ⚡ `electionDetail-load-parallel-002` 并行 `getPositions` `getCandidates` `getNotices` `getMaterials` `getNotifications`（均带 `{electionId}`）（`detail:465-471`）
- ⚡ `electionDetail-pos-crud-003` `createPosition`/`updatePosition`/`deletePosition`（`detail:540-552`）
- ⚡ `electionDetail-cand-crud-004` `createCandidate`(→import)/`updateCandidate`(→review)/`deleteCandidate⚠️`（`detail:587-599`）
- ⚡ `electionDetail-cand-pass-005` `updateCandidate(id,{status:'已通过'})` → `/candidate-v2/review`（`detail:603`）
- ⚡ `electionDetail-cand-io-006` `importCandidates⚠️` `exportCandidates⚠️`（`detail:613,624`）
- ⚡ `electionDetail-mat-review-007` `reviewMaterial(id,{action:'approve'|'reject',reason})` → `POST /material-v2/review`（`detail:637-638`）
- ⚡ `electionDetail-notice-crud-008` `createNotice`/`updateNotice`/`deleteNotice`/`publishNotice`（`detail:389`）
- 💾 `electionDetail-bind-009` 全部按 `x.data || []` 当**数组**（`detail:472-476`）。
- 💾 `electionDetail-bind-aggr-010` 用 `positionId` 聚合提交数与通过数（`detail:480-487`）。候选人字段：`name` `position` `village` `status`；职位字段：`name` `quota`。
- ⚠️ `electionDetail-risk-deleteCand-011` `deleteCandidate` → `DELETE /admin/candidates/{id}`（@@TODO-无后端，candidate-v2 无 delete 端点）。

### 职位管理 `views/positions/index.vue`（CrudPage + 活动选择器）
- ⚡ `position-ctx-getElections-001` `getElections()` 填活动下拉（`positions:170`）
- ⚡ `position-list-getPositions-002` `getPositions({electionId})` → `GET /position-v2/list`（`positions:235`）
- ⚡ `position-crud-003` create→`/position-v2/add`、update→`/position-v2/update`、delete→`/position-v2/delete`
- 💾 `position-bind-elections-004` `res.data || []` 当数组（`positions:171`）。
- 💾 `position-bind-form-005` 表单字段：`name` `duty` `materialRequirements[]`（每项 name/required/formats[]/description/sampleImage）`quota` `sortWeight` `enabled`（`positions:36-89`）。
- ⚠️ `position-risk-matReq-006` `materialRequirements` 期望是**数组**；后端若回 JSON 字符串需前端兜底解析（detail/approvals 页有 `JSON.parse` 兜底，positions 页未见显式兜底）。
- ⚠️ `position-risk-upload-007` 示例图上传用 `UPLOAD_URL`（`/api/upload`）+ `uploadHeaders`，回填 `resp` → 需确认取的是 `resp.data.xxx` 还是 `resp.url`。

### 候选人管理 `views/candidates/index.vue`（CrudPage + 活动选择器）
- ⚡ `candidate-ctx-getElections-001` `getElections()`（`candidates:154`）
- ⚡ `candidate-ctx-getVillages-002` `getVillages()`（`candidates:158`）
- ⚡ `candidate-ctx-getPositions-003` `getPositions({electionId})`（`candidates:173`）
- ⚡ `candidate-list-getCandidates-004` `getCandidates({electionId})` → `GET /candidate-v2/list`（`candidates:270`）
- ⚡ `candidate-crud-create-005` `createCandidate` → `POST /candidate-v2/import`（后台录入=import来源，`api.ts:68`）
- ⚡ `candidate-crud-update-006` `updateCandidate` → `POST /candidate-v2/review`（`api.ts:72`）
- ⚡ `candidate-io-007` `importCandidates⚠️` `exportCandidates⚠️` `resubmitCandidate⚠️` `checkCandidateMaterials⚠️`（均 @@TODO-无后端）
- 💾 `candidate-bind-008` 列表 `res.data || []`、职位 `res.data||[]` map 处理（`candidates:155,174,270`）。状态枚举：报名中/已公示/资格作废（`candidates:265`）。
- ⚠️ `candidate-risk-import-009` `importCandidates` 用 `res.message`（`candidates:205`），应为 `res.data.message` 或 `res.msg`。
- ⚠️ `candidate-risk-statusMismatch-010` 列表 tagMap 用「报名中/已公示/资格作废」，但 detail 页用「待审核/已通过」。**同一候选人 status 枚举两套**，需与后端核对真值。

### 审批管理 `views/approvals/index.vue`
- ⚡ `approval-load-getElections-001` `getElections()`（`approvals:261`）
- ⚡ `approval-load-getAdmins-002` `getAdmins()⚠️` 填经办人下拉（`approvals:272`）
- ⚡ `approval-load-getPositions-003` `getPositions({electionId})`（`approvals:280`）
- ⚡ `approval-mat-review-004` `reviewMaterial(id,{action:'approve'/'reject',reason})`（`approvals:357,450`）
- ⚡ `approval-cand-update-005` `updateCandidate(id,{...})` 多处用于状态流转（`approvals:383,418,454,461`）
- 💾 `approval-bind-006` `eRes.data||[]`、`aRes.data||aRes||[]`、`posRes.data||[]`（`approvals:262,273,281`）。
- 💾 `approval-bind-aggr-007` materials 按 `applicantName|applicantPhone|positionId` 聚合成报名人（`approvals` loadApplicants 同 materials 页逻辑）。
- ⚠️ `approval-risk-matReq-008` `materialRequirements` 做 `Array.isArray ? : JSON.parse兜底`（`approvals:284-286`）——说明后端格式不稳定，前端已埋兜底。

### 材料审核 `views/materials/index.vue`
- ⚡ `material-load-getElections-001` `getElections()`（`materials:206`）
- ⚡ `material-load-getPositions-002` `getPositions({electionId})`（`materials:223`）
- ⚡ `material-load-getMaterials-003` `getMaterials({electionId})` → `GET /material-v2/list`（`materials:238`）
- ⚡ `material-review-004` `reviewMaterial(id,{action,reason})` → `POST /material-v2/review`（`materials:348,365`）
- 💾 `material-bind-005` 全 `res.data || []` 当数组（`materials:207,224,239`）。
- 💾 `material-bind-aggr-006` 按 `applicantName|applicantPhone|positionId` 聚合，材料字段：`applicantName` `applicantPhone` `positionId`（`materials:244-257`）。
- ⚠️ `material-risk-status-007` 报名人选「进行中」用 `e.status==='in_progress'`（`materials:210`）——英文枚举假设。

### 公告管理 `views/notices/index.vue`（CrudPage + 活动选择器 + tiptap）
- ⚡ `notice-ctx-getElections-001` `getElections()`（`notices:150`）
- ⚡ `notice-list-getNotices-002` `getNotices({...params,electionId})` → `GET /notice-v2/list`（`notices:182`）
- ⚡ `notice-crud-003` create→`/notice-v2/add`、update→`/notice-v2/update`、delete→`/notice-v2/delete`（`notices:183-185`）
- ⚡ `notice-publish-004` `publishNotice(id)` → `POST /notice-v2/publish`（`notices:167`）
- ⚡ `notice-upload-005` `http.post('/upload',fd)` 插图（`notices:128`）
- 💾 `notice-bind-006` 列表经 CrudPage 兼容。富文本 `content` 双向同步 tiptap（`notices:99-111`）。
- ⚠️ `notice-risk-publishId-007` 发布后 `if (res && res.id)`（`notices:167-168`）应为 `res.data.id` → 发布成功判断恒 false。
- ⚠️ `notice-risk-uploadUrl-008` 插图 `res.url`（`notices:129`）应为 `res.data.url`。

### 通知设置 `views/notifications/index.vue`
- ⚡ `notification-list-getNotifications-001` `getNotifications(params)` → `GET /notification-v2/list`（`notifications:205`）
- ⚡ `notification-create-002` `createNotification` → `POST /notification-v2/send`（`notifications:230`）
- ⚡ `notification-sendNow-003` `sendNotificationNow(id)` → `POST /notification-v2/send`（`notifications:240`）
- ⚡ `notification-export-004` `exportNotifications⚠️`、辅助 `getElections`/`getVillages`/`getRoles⚠️`/`getAdmins⚠️`
- 💾 `notification-bind-005` `tableData.value = res.data || []` 当数组（`notifications:206`）。
- ⚠️ `notification-risk-send-006` `sendNotificationNow` 与 `createNotification` 共用 `/notification-v2/send`，重发语义未确认（`api.ts:162`）。

### 选举方式 `views/election-methods/index.vue`
- ⚡ `electionMethod-list-001` `getElectionMethods({status})⚠️` → `GET /admin/election-methods`（`election-methods:117`）
- ⚡ `electionMethod-crud-002` `createElectionMethod⚠️`/`updateElectionMethod⚠️`/`deleteElectionMethod⚠️`/`reviewElectionMethod⚠️`（`election-methods:142-184`）
- 💾 `electionMethod-bind-003` `res.data || []` 当数组（`election-methods:118`）。
- ⚠️ `electionMethod-risk-004` 整页 5 个接口全 @@TODO-无后端，且 api.ts 注释「废弃？选举方式已是 elections 字段」。**该页与 election/index 的方式下拉同源同患**。

### 历史归档 `views/archives/index.vue`（CrudPage）
- ⚡ `archive-list-001` `getArchives⚠️` → `GET /admin/archives`
- ⚡ `archive-crud-002` create/update/delete⚠️（`/admin/archives`）
- ⚡ `archive-export-003` `exportArchives⚠️`(`{year}`)/`exportFullArchives⚠️`，`responseType:'blob'`（`archives:55,63`）
- 💾 `archive-bind-004` 经 CrudPage；字段 `name` `year` `type` `fileCount` `date`（`archives:24-28`）。导出直接当 blob 用（`archives:54-55`）。⚠️ 拦截器对 blob 不走 `{code,msg,data}` 解包，需后端确认导出走原始流。
- 🔐 整页全 @@TODO-无后端。

### 系统人员 `views/admins/index.vue`（CrudPage）
- ⚡ `admin-roles-getRoles-001` `getRoles()⚠️` 填角色下拉（`admins:12`）
- ⚡ `admin-crud-002` `getAdmins⚠️`/`createAdmin⚠️`/`updateAdmin⚠️`/`deleteAdmin⚠️`
- 💾 `admin-bind-003` `res.data || res || []`（`admins:13`）——三段兜底。字段 `name` `role` `phone` `lastLoginAt`（`admins:21-29`）。
- 🔐 `admin-roles-004` 仅「超级管理」可访问（`router:29`）。

### 角色权限 `views/roles/index.vue`（CrudPage）
- ⚡ `role-crud-001` `getRoles⚠️`/`createRole⚠️`/`updateRole⚠️`/`deleteRole⚠️`（`api.ts:239-252`）
- 💾 `role-bind-002` 经 CrudPage；字段 `name` `description` `count`（`roles:8-10`）。
- 🔐 仅「超级管理」。

### 系统设置 `views/settings/index.vue`
- ⚡ `setting-get-001` `getSettings()⚠️` → `GET /admin/settings`（`settings:150`）
- ⚡ `setting-update-002` `updateSettings(data)⚠️` → `PUT /admin/settings`（`settings:166`）
- 💾 `setting-bind-003` `res.data` 填 form（`settings:150`）。
- 🔐 仅「超级管理」。

### 操作日志 `views/logs/index.vue`（CrudPage 只读）
- ⚡ `log-list-001` `getOperationLogs⚠️` → `GET /log-v2/list`（`api.ts:117`，注释 koaLite 暂无 logs 路由）
- 💾 `log-bind-002` 经 CrudPage；字段 `createdAt` `actorName` `action` `module` `detail`（`logs:8-12`）。
- 🔐 超级管理/经办。

### H5 村民端 `views/mobile/*`（免登录）
- ⚡ `mobile-home-getNotices-001` `miniGetNotices()⚠️` → `GET /mini/notices`（`Home:89`）
- ⚡ `mobile-home-getElections-002` `miniGetElections()⚠️` → `GET /mini/elections`（`Home:94`）
- ⚡ `mobile-candidates-003` `miniGetCandidates(electionId)⚠️` → `GET /mini/candidates`（`Candidates:101`）
- ⚡ `mobile-electionMethods-004` `miniGetElectionMethods()⚠️` → `GET /mini/election-methods`（`Elections:46`）
- 💾 `mobile-bind-005` 统一 `res?.data || res || []` 三段兜底（`Home:90,95` `Candidates:102,114`）——比管理端更宽容。
- 🔐 `mobile-auth-006` router `beforeEach` 对 `/m` 开头**直接放行免登录**（`router:56`）。⚠️ `/mini/*` 全部 @@TODO-无后端待建。

---

## 3. Mermaid 前端结构图

```mermaid
graph LR
  subgraph 管理端_CrudPage统一组件
    V_village[村居] --> CP[CrudPage.vue]
    V_arch[归档] --> CP
    V_admin[系统人员] --> CP
    V_role[角色] --> CP
    V_log[日志只读] --> CP
    V_pos[职位] --> CP
    V_cand[候选人] --> CP
    V_notice[公告] --> CP
  end

  subgraph 管理端_手写页面
    V_login[登录]
    V_dash[首页概览]
    V_elec[选举管理]
    V_edetail[选举详情]
    V_appr[审批]
    V_mat[材料审核]
    V_notif[通知]
    V_method[选举方式]
    V_set[系统设置]
  end

  subgraph 移动端H5_免登录
    M_home[H5首页]
    M_notice[H5公告]
    M_elec[H5选举方式]
    M_cand[H5候选人]
  end

  CP -->|list/create/update/delete| API[api/api.ts]
  V_login --> API
  V_dash --> API
  V_elec --> API
  V_edetail --> API
  V_appr --> API
  V_mat --> API
  V_notif --> API
  V_method --> API
  V_set --> API
  M_home --> API
  M_notice --> API
  M_elec --> API
  M_cand --> API

  API --> HTTP[api/index.ts<br/>baseURL=/api<br/>解包 code/msg/data]

  HTTP -->|有后端 schema-v2| OK[/auth-v2 election-v2 village-v2<br/>position-v2 candidate-v2 material-v2<br/>notice-v2 notification-v2/]
  HTTP -.->|@@TODO-无后端| TODO[/admin/* admin-v2/*<br/>log-v2/list mini/*/]

  style TODO fill:#fde2e2,stroke:#b91c1c
  style OK fill:#dcfce7,stroke:#16a34a
```

---

## 4. 关键发现

### 4.1 CrudPage 统一组件：改一处影响 8 页
用 CrudPage 的页面：**村居、归档、系统人员、角色、日志、职位、候选人、公告**（职位/候选人/公告还套了「活动选择器」外壳）。
- CrudPage 的 `fetchData`（`CrudPage.vue:124-141`）是全系统**唯一**对返回格式做双轨兼容（数组 / `{list,total}`）的地方。
- 改动 CrudPage 的取数、分页、表单初始化逻辑会同时波及这 8 页。提交逻辑见 `handleSubmit`（`CrudPage.vue:165-183`）：编辑时过滤空值后调 `update`，新增调 `create`。

### 4.2 数据格式假设：管理端手写页全假设 `res.data` 是数组
- **手写页面**（dashboard/election/detail/approvals/materials/notifications/election-methods/settings）一律 `res.data || []`，**不兼容** `{list,total}` 分页结构。
- **CrudPage 页面**两种都兼容。
- **H5 页面**最宽容：`res?.data || res || []`。
- 结论：若后端某接口改成分页 `{list,total}` 返回，**手写页会集体取不到数据**（拿到对象当数组 → 列表空）。这是前后端契约最大风险面。

### 4.3 `res.id` / `res.url` / `res.message` 取值错位（确认级 Bug）
拦截器返回整个 `{code,msg,data}`，真实数据在 `res.data`。但以下处写成顶层属性：
- `election/index.vue:386-387` `res.id`（新建选举接 id）→ 应 `res.data.id`
- `election/index.vue:263` `res.url`（富文本插图）→ 应 `res.data.url`
- `notices/index.vue:167-168` `res.id`（发布成功判断）→ 应 `res.data.id`
- `notices/index.vue:129` `res.url`（插图）→ 应 `res.data.url`
- `candidates/index.vue:205` `res.message`（导入提示）→ 应 `res.data.message` 或 `res.msg`
这些会导致：新建选举后接不上 id、公告发布判定永远 false、富文本图片插不进去。**需后端一起核对，或前端统一改 `res.data`。**

### 4.4 候选人 status 枚举两套（前端内部就不一致）
- 列表页 `candidates/index.vue:265`：报名中 / 已公示 / 资格作废
- 详情页 `election/detail.vue`：待审核 / 已通过
同一实体 status 在两页用不同枚举集，需与后端 `candidate-v2` 真值对齐。

### 4.5 选举状态枚举中英混判
dashboard、election、materials 等多处同时判 `'in_progress'` 与 `'进行中'`、`'published'` 与 `'已发布'`。前端自己也不确定后端回中文还是英文，埋了双轨兜底。**建议后端定一套，文档化。**

### 4.6 标了 `@@TODO-无后端` 的 api 函数（前端在调但后端没有）
整页全是无后端的：**选举方式、归档、系统人员、角色、系统设置、操作日志、全部 H5 村民端**。
具体函数（`api.ts` 行号）：
- 选举方式：`getElectionMethods`(44) `createElectionMethod`(46) `updateElectionMethod`(48) `reviewElectionMethod`(50) `deleteElectionMethod`(52) — 注释疑「已废弃，方式已是 elections 字段」
- 系统设置：`getSettings`(56) `updateSettings`(58)
- 候选人扩展：`deleteCandidate`(81) `resubmitCandidate`(90) `checkCandidateMaterials`(95) `importCandidates`(98) `exportCandidates`(108)
- 日志：`getOperationLogs`(117) `exportOperationLogs`(122) — 注 logs 表有数据待建查询路由
- 材料扩展：`updateMaterial`(136) `deleteMaterial`(140)
- 通知扩展：`updateNotification`(157) `exportNotifications`(167) `getNotification`(171)
- 归档：`getArchives`(194) `createArchive`(198) `updateArchive`(202) `deleteArchive`(206) `exportArchives`(211) `exportFullArchives`(215)
- 管理员：`getAdmins`(221) `createAdmin`(225) `updateAdmin`(229) `deleteAdmin`(233)
- 角色：`getRoles`(239) `createRole`(243) `updateRole`(247) `deleteRole`(251)
- H5 村民端：`miniGetNotices`(263) `miniGetElections`(267) `miniGetCandidates`(271) `miniGetElectionMethods`(275) `miniGetNotifications`(279)

⚠️ 高优先级矛盾：**选举管理新建表单**（`election/index.vue`）的「选举方式」下拉依赖无后端的 `getElectionMethods`。这是 schema-v2 已落地的核心建选举流程踩在一个 @@TODO 接口上。

### 4.7 接口风格分裂：schema-v2 RESTish vs 旧 `/admin/*`
- 有后端的走 `xxx-v2/动词`（`/election-v2/add`、`/material-v2/review`），全 POST + body 带 id。
- 无后端的走旧 `/admin/xxx` 或 `/admin-v2/xxx`，是标准 REST（`PUT /admin/roles/{id}`、`DELETE /admin/candidates/{id}`）。
两套风格并存，迁移时旧 REST 风格需改写为 v2 动词风格或后端补 REST 路由。

### 4.8 2026-06-30 主流程页面巡检修复（已实测）
- `election-index-methods-20260630`：`views/election/index.vue` 不再调用旧 `getElectionMethods()`/`/admin/election-methods`，改用 `getElectionMethodMap()` → `/election-v2/methods`。方式下拉由字典对象展开，提交字段用 `electionType/electionMethod`。
- `position-list-shape-20260630`：`views/positions/index.vue` 的岗位列表适配后端统一 `{list,total}`，不再对 `res.data` 直接 `forEach`；同时把 `material_requirements/elected_candidates` 归一到 `materialRequirements/electedCandidates`。
- `notification-meta-20260630`：`views/notifications/index.vue` 首屏不再等待未迁移的 `/admin/roles`、`/admin/admins`。角色使用中文四角色本地常量，系统人员下拉暂为空，不阻塞通知列表。
- `notice-global-20260630`：`views/notices/index.vue` 列表不再强制传当前 `electionId`，可显示 `election_id=null` 的全局公告；新增/编辑仍带当前活动上下文。
- `sidebar-methods-20260630`：`layouts/Sidebar.vue` 隐藏废弃的"选举方式"菜单入口。该概念已归并为选举活动字段，不再是独立实体页。
- 验证证据：Playwright 登录 `13800000001/123456/超级管理` 后访问 `/villages /election /positions /candidates /notices /notifications`，表格行数分别为 `2/7/3/3/1/3`，均无加载失败；`npm exec vite build` 通过。
- 遗留：`election-methods` 页面文件仍在但无菜单入口；`archives/admins/roles/settings/logs/mini` 仍属于 @@TODO-无后端或待决策模块。

### 4.9 2026-06-30 压实检查新增 Bug 清单
- `auth-token-store-20260630`：`stores/auth.ts` 仍按 `res.token/res.user` 存储登录结果；当前 axios 拦截器返回 `{code,msg,data}`，正确为 `res.data.token/res.data.user`。GET 读放开掩盖了问题，写操作会带错 token。
- `api-status-machine-20260630`：`api.ts` 中 `updateElectionStatus`、`updateCandidate/updateCandidateStatus`、`sendNotificationNow` 仍是旧状态机思路。后端要求：election approve=`action approve/reject`；candidate review=`action pass/fail`；notification send=新建通知不是重发。
- `material-reject-key-20260630`：`election/detail.vue` 驳回材料传 `{reason}`，后端 `material-v2/review` 要 `{rejectReason}`。
- `field-snake-camel-20260630`：后端 list 返回 snake_case；前端多页仍读 camelCase。浏览器样本已见：候选人页职位/村居/推荐方式为空，材料/审批聚合可能按 `undefined` 分组，通知范围/时间为空。
- `old-api-buttons-20260630`：候选人页仍暴露导入/导出/删除/重新提审/材料上下文等旧 `/admin/candidates/*` 按钮；系统管理/归档/H5 多模块仍指向无后端接口。
- `upload-path-20260630`：后端上传是 `/upload/saveFile`，前端富文本和职位示例图仍使用 `/upload` 或 `/api/upload`。
- `build-toolchain-20260630`：`npm run build` 被 `vue-tsc@1.8.27 + typescript@5.9.3 + Node 24` 卡死，完整 TS 校验不可用；`npm exec vite build` 可通过。

### 4.10 2026-06-30 P0/P1 修复回收
- `api-adapter-20260630`：新增 `admin/src/api/adapter.ts`。责任：只做前后端契约适配（字段别名、payload 翻译），不做业务状态机决策；这是 snake/camel 的唯一前端真相源。
- `auth-token-store-20260630`：`stores/auth.ts` 已改用 `pickLoginPayload(res)`，从 `res.data.token/res.data.user` 存储登录态。Playwright 验证 token 非 `undefined`。
- `api-list-normalize-20260630`：`getElections/getElection/getPositions/getMaterials/getCandidates/getNotices/getNotifications` 返回值统一过 `normalizeResponse`，页面可继续读 camelCase，同时保留后端 snake_case 原字段。
- `api-write-payload-20260630`：`updateElectionStatus/updateCandidate/updateCandidateStatus/reviewMaterial/createNotification` 已统一翻译成 koaLite v2 真实入参。旧页面传 `reason/phones/中文状态` 不再直接打给后端。
- `old-api-entry-prune-20260630`：候选人页隐藏导入/导出/删除/材料上下文/编辑等无后端或危险误操作入口；选举详情页隐藏候选人导入/导出/删除/编辑；通知页隐藏导出和“立即发送已有通知”；侧栏隐藏 archives/admins/roles/settings/logs。
- `upload-path-20260630`：`election/index.vue`、`notices/index.vue` 富文本上传，以及 `positions/index.vue` 示例图上传，统一改 `/api/upload/saveFile`，成功取 `resp.data.url`。
- `approval-admins-20260630`：`approvals/index.vue` 不再加载旧 `/admin/admins`，用当前登录人兜底，首屏无 404。
- 验证证据：
  - `node admin/scripts/contract-smoke.mjs` → `contract-smoke ok`
  - `npm exec vite build` → 通过
  - Playwright 登录巡检主线页面和 `/election/1`：`/dashboard=1 /villages=2 /election=7 /positions=3 /materials=2 /candidates=3 /approvals=0 /notices=1 /notifications=3 /election/1=11`；均无加载失败、无 4xx API。
- 未清理但已隔离：`api.ts` 仍保留若干 @@TODO-无后端函数定义，作为后续 P2 决策锚点；当前主线 UI 不再暴露这些旧接口入口。

---

## 5. 锚点统计

| 类别 | 数量 |
|---|---|
| ⚡ API 接入点 | 71 |
| 💾 数据绑定 | 31 |
| 🔐 权限 | 8 |
| ⚠️ 风险点 | 23 |
| **总计** | **133** |

> 风险点中确认级 Bug 5 个（res.id/url/message 错位），格式假设类 1 个（手写页只认数组），枚举不一致 2 个（候选人 status、选举状态中英）。
