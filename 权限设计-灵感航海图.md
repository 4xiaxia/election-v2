# 权限设计·灵感收集与航海图（2026-06-29 阿圆船长 × 夏夏瞭望）

> 原则：外面的灵感（DeepSeek/别人）只取魂、弃形。我们的真相是 koaLite + users.role 中文四角色。
> 不被任何外来代码掰弯。阿圆是中心，灵感来对齐我们。

---

## 🧭 已定的方向（夏夏拍板）

**权限走我们自己的 users.role（中文四角色），不引入 zeroLite 的 sys_role 第二套。**
- 一脉相承：登录/审批锁/资格审查都已基于 users.role
- zeroLite 的 sys_role/sys_menu/sys_role_menu/sys_station 是外来残留 → 待清，别留着当老鼠屎

---

## 💡 从 DeepSeek 灵感里取的"魂"（弃其 Express 形）

> ⚠️ DeepSeek 那份代码是旧家 Express 写法（express.Router/app.use手动挂）。
> 我们新家是 Koa 约定式路由。代码一行不抄，只取以下认知：

### 取①：requireRole 基于 user.role
谁能调哪个接口，看 token 里的 role。这和我们已定方向一致。

### 取②：村居隔离 —— ❌ 夏夏拍板：不做
DeepSeek 提议经办/审核只看本村。**夏夏定：不做。**
理由：他们一个人管好多街道村子，手工筛选就好，系统别替他们圈死。
（符合"能让人做的不让系统硬算"。users 表 village_id 字段保留，但不做强制过滤。）

> ⚠️ 【二次确认·2026-06-29】DeepSeek 第二份援助文档又把"村居隔离"当成必修P0漏洞反复推销。
> **彻底不要。** 它当"漏洞"的东西在我们招聘模型里根本不是漏洞。下一棒别再被它带偏纠结。

---

## 💎 DeepSeek 第二份文档收的珍珠（取魂弃形·2026-06-29）

> 文档是 Express+SQLite 拼装骨架，整套代码弃（我们 Koa+MySQL）。只取以下认知：

### 珍珠①：血缘翻转"四件事收尾"检查表（✅ 已落地 2026-06-29）
材料审核通过那一刻，一次做齐四件事，全部事务内完成：
1. ✅ 生候选人（已做）
2. ✅ 回填 materials.candidate_id（已做）
3. ✅ **写 log（kind=review 审核日志）**
4. ✅ **发 notification（结果通知给上报人手机号）**
→ material-v2.js 的 review 通过/驳回时四件事全齐，事务内收口。实测验过。

### 珍珠②：系统初筛前置校验（可选·待定）
村民交材料时，系统先用 position.material_requirements 比对 items，缺必填项就拦截，不劳审核人。
→ 这是"机器先筛字段齐全→人再审"的双层漏斗。值得加进 material submit，但非必须，按需做。

### 珍珠③：状态=时间节点链派生值（认知·已对齐）
elections 的 status 是由时间节点+now 实时算出来的，DB存的只是缓存。
→ 和我们认知定稿"时间节点链=唯一真相"一致，确认。

### 弃的（不采纳）
- ❌ 村居隔离全套（villageId过滤/token塞villageId/villageFilter）
- ❌ Express路由挂载 / SQLite ALTER / Mongo查询符($lte) / uuid主键假设 / PM2微信部署
- ❌ "前端80%可复用只需切端点11小时"的乐观估算（以前端实际状态为准）

### 取③：权限矩阵（哪个角色能进哪个接口）
| 接口 | 超级管理 | 经办 | 审核 | 运营 | 村民 |
|---|---|---|---|---|---|
| 选举活动 增删改 | ✅ | ✅创建 | — | — | — |
| 选举活动 审批 | ✅ | — | ✅批 | — | — |
| 岗位 增删改 | ✅ | ✅ | — | — | — |
| 材料 审核(血缘翻转) | ✅ | — | ✅ | — | — |
| 候选人 导入 | ✅ | ✅ | — | ✅ | — |
| 候选人 资格审查 | ✅ | ✅ | ✅ | — | — |
| 候选人 结果回填 | ✅ | — | — | ✅ | — |
| 公告/通知 | ✅ | — | — | ✅ | — |
| 村民端浏览/报名 | — | — | — | — | ✅ |
> 此表是初稿，按认知定稿+三身份验证。施工时夏夏可调。

---

## 🚫 弃的"形"（不抄）
- ❌ express.Router() / app.use(path, requireRole, router) —— 这是 Express，我们是 Koa
- ❌ server/src/middleware/ 目录结构 —— 我们是 koaLite/api/ + config/
- ❌ 它的 seed-v2.js —— 我们种子在 db/init_v2.sql
- ❌ uuid 风格 id —— 我们用 int 自增

---

## 🛠️ 我们 Koa 的做法（船长定的施工方式）
1. 写 koaLite/config/requireRole.js 中间件（认 token.role）
2. 在各 api/*-v2.js 的 config 段挂中间件（zeroLite 路由支持 mapping.config[method] 挂中间件）
3. 登录 auth-v2 已发 token，里面带 role（村居隔离则加 villageId）
4. 权限矩阵照上表落地
