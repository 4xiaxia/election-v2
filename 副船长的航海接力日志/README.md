# 副船长的航海接力日志

用途：只放真实、可接力、可验证的短断点；不复制长聊天，不替代根目录真相源。

## 当前主线

```mermaid
flowchart LR
    A[村/社区归属地] --> B[换届母档案]
    B --> C[主任/副主任/委员]
    C --> D[候选人报名/导入]
    D --> E[材料审核]
    E --> F[公告/通知/归档]
```

## 已验事实

- 岗位自动生成后端主干已有。
- `village 3 => 主任1 + 委员2` 按“小规模村可不设副主任”的新决策成立。
- 旧岗位数据不删，历史查看走 `includeAll=1`。

## 当前断点

- 前端还没接 `generatePositions`。
- 职位页还没有“一键生成岗位”入口。
- `orgType / committeeSize / deputyCount` 的前端来源要先查清楚。

## 下一刀

```mermaid
flowchart LR
    A[查字段来源] --> B[补前端API]
    B --> C[补职位页按钮]
    C --> D[刷新岗位列表]
    D --> E[验证并记录]
```

## 纪律

先图后文；先边界后代码；少即是美；运筹而不是新造。
